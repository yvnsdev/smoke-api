services:
  - type: web
    name: smoke-detection-app
    env: docker
    dockerfilePath: Dockerfile.combined
    branch: main
    envVars:
      - key: LOG_LEVEL
        value: INFO
      - key: SAMPLE_EVERY
        value: 5
